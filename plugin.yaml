name: refine
version: 0.11.0
description: "Measurement layer for Hermes self-improvement: finds failures that recur across sessions, proposes one minimal skill/memory/prompt edit, and grades whether the fix held."
author: Bergschloss
provides_tools: [refine_run]
provides_hooks: [pre_llm_call, pre_tool_call, post_llm_call, on_session_end, on_session_reset, subagent_start, subagent_stop]
kind: standalone
