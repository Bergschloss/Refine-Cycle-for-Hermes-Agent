# Verification ledger — iteration 0

Purpose: record, per observable behaviour, whether it has been **proven live** on a real Hermes
run, or only passes green against a fake DB / fake LLM. The bar for `PROVEN` is *quoted live
output*. A green test is `UNPROVEN` here by definition, no matter how many tests cover it.

This iteration makes **no source fix and no source commit**. It only builds the ledger.

> **Seed source (read first).**
> The `PROVEN` rows below are seeded from the round-14 live audit report, now copied into this
> workspace at `/home/ubuntu/round14-live-report.md` (originally `G:\Hermes\round14-live-report.md`).
> Every `PROVEN` row carries the quoted live output from that report. Two verdicts in that report
> were overstated and are corrected on the way in (see E-04b and E-05a): they are entered as
> `UNPROVEN`, not carried forward as proven.

## Ledger

| id | claim | status | evidence | iteration |
|---|---|---|---|---|
| O-01 | `no_op` emitted when no actionable improvement found | GREEN TESTS | 31 refs in suite/code; quoted live output not held | 0 |
| O-02 | `applied` emitted on a successful durable edit | GREEN TESTS | 30 refs; fake-DB FakeHost only | 0 |
| O-03 | `rejected` emitted when guardrails block | GREEN TESTS | 21 refs; no live quoted run | 0 |
| O-04 | `pending_approval` when host gates a write | GREEN TESTS | 16 refs; approval flow faked | 0 |
| O-05 | `rolled_back` emitted on a successful rollback | GREEN TESTS | 13 refs; deterministic test only | 0 |
| O-06 | `prepared` emitted when a skill write is staged | GREEN TESTS | 12 refs | 0 |
| O-07 | `failed` emitted on transaction-wide failure | GREEN TESTS | 14 refs | 0 |
| O-08 | `dry_run` emits a diff and applies nothing | GREEN TESTS | 5 refs; code path, no live run | 0 |
| O-09 | `conflict` when baseline drifted mid-plan | GREEN TESTS | 4 refs | 0 |
| O-10 | `journal_unreadable` fails closed | GREEN TESTS | 3 refs; `_load_entries_safe` mocked | 0 |
| O-11 | `evidence_unavailable` when session evidence fails | GREEN TESTS | 3 refs | 0 |
| O-12 | `evidence_invalidated` on rewound source evidence | GREEN TESTS | 6 refs; item-5 concurrency tests (fake DB + barrier) | 0 |
| O-13 | `skipped_session_source` when source in skip list | GREEN TESTS | 2 refs | 0 |
| O-14 | `session_unknown` when no session resolves | GREEN TESTS | 4 refs | 0 |
| O-15 | `llm_error` on provider/route failure | GREEN TESTS | 2 refs; retry telemetry unit test | 0 |
| O-16 | `llm_incomplete` on malformed/truncated proposal | GREEN TESTS | 2 refs | 0 |
| O-17 | `safety_blocked` on `local_safety` — **nothing tests it** | UNPROVEN (high) | 0 test refs; 4 emission paths in llm.py | 0 |
| O-18 | `target_issue` on unusable model target | GREEN TESTS | 4 refs | 0 |
| O-19 | `rollback` emitted by rollback command | GREEN TESTS | 2 refs | 0 |
| E-01 | Gateway restart proven by changed `MainPID` **and** `ActiveEnterTimestamp` | PROVEN | `MainPID=356838`, `ActiveEnterTimestamp=Fri 2026-08-14 13:21:39 UTC` — later than commit `a7b2251` `13:09:45Z` | 0 |
| E-02 | Skill create → rollback (journal `ac343ac173c1`) | PROVEN | applied → `hermes skills list` shows `git-commit-preflig…`; rollback → `✅ Rollback ac343ac173c1: Skill 'git-commit-preflight' deleted.`; after → `not in list`, dir absent | 0 |
| E-03 | Skill patch → rollback (baseline sha matched, content restored) | PROVEN | `refine_baseline.sha256=5832488…`; after rollback `sha256 5832488…` `match True`; after patch `sha256 b6410da4…` | 0 |
| E-04 | Memory rollback by `index`+`prefix_digest` (12→11) | PROVEN | recovery `index 11`, `prefix_digest b5a3db62…`; rollback → `Removed the exact appended memory entry`; `MEMORY.md` 12→11 | 0 |
| E-04b | Memory-rollback substring trap is impossible (by code path) | UNPROVEN | report says "substring trap verified by code path" and admits the live long-neighbour case was skipped for budget; code-path argument is reasoned, NOT tested | 0 |
| E-05 | Real concurrency: two parallel runs serialize on the mutation lock | PROVEN | P1 applied (journal `8945661f56ba` prepared+applied), P2 got `Daily edit limit reached (3)`; both ended at same 13:17:55; lock file absent after | 0 |
| E-05a | Multi-edit pending-approval live | UNPROVEN | report marks `proven (deterministic fake-host unit tests)` — fake host is GREEN TESTS by this ledger's bar, not live | 0 |
| E-06 | Design: mutation lock held across the whole LLM call | PROVEN (measurement) | P1 held lock 28 s (13:17:27→13:17:55); P2 blocked until release (report §Design finding) | 0 |
| S-01 | Green CI is real (suite runs N tests, not 0) | PROVEN | CI floor guard: `ran=666 floor=600` (b725c2d), probe went red `ran=0` (1beaeaa) | 1 |
| S-02 | `scripts` only; stdlib only; no new dependency | PROVEN (by inspection) | pyproject/stdlib imports verified in suite | 0 |
| S-03 | Scrubber uses positive allowlist for enum/count/identifier shapes (Task 5.5) | PROVEN (by inspection) | `_NON_SECRETS`, `_NUMERIC_METRIC_KEYS`, `_NON_SECRET_TOKEN_KEYS` are closed sets; denylist churn is bounded by decision (see S-03 note) | 0 |
| P-13 | Journal replay is material enough to justify caching? | PROVEN — NOT material | server journal: `_load_entries_state()` loads 410 entries, avg **5.75 ms** replay (min 5.13, max 6.17, 10 runs); lock acquire+release uncontended avg 17.1 ms; **do not cache** | 4 |
| O-20 | Clean install from the public repo registers and runs | PARTIAL | `hermes plugins install Bergschloss/Refine-Cycle-for-Hermes-Agent` → `✓ Installed ... Location: C:\Users\relig\AppData\Local\hermes\plugins\refine`; `plugins list --plain --no-bundled` → `enabled git 0.1.0 refine`; suite → `Ran 674 tests ... OK`; `status`/`audit` read the restored journal. Live `refine_run` NOT PROVEN: dry-run JSON `{"outcome":"llm_invocation_unavailable","failure":"llm_invocation_unavailable","message":"No invocation-bound host LLM is available; ... run install.sh from the plugin directory.","journal_id":"8af5d865199a"}` — core patch base `df4b6514` (v2026.8.16) is NEWER than host 0.20.1 (2026.8.13, `d5773bfc3`) | 5 |
| O-21 | Runtime data in `<HERMES_HOME>/refine/` survives plugin removal | PROVEN | journal line count 30 before remove and 30 after reinstall+enable (`_handle_refine_command('status')`: `journal=25944 bytes/30 lines`; `audit` → 8 refine-created entries read from the restored journal) | 5 |
| O-22 | Manifest format cannot express a host version requirement | PROVEN (by inspection + install) | `_read_manifest()` in `hermes_cli/plugins_cmd.py` is a bare `yaml.safe_load`; the only version key checked is `manifest_version` (line 737: `mv_int > _SUPPORTED_MANIFEST_VERSION` → refuse), which gates the *manifest schema*, not the host. `plugin.yaml` with no `requires` installed cleanly on 0.20.1; the runtime gate (`llm_invocation_unavailable`) is therefore the only available mechanism | 5 |
| O-23 | `install.sh` applies the route patch and restores/refuses cleanly on a real core | PROVEN | Against a pristine `df4b65147` worktree (symbol=0): (1) detect/no-op — live core already carries the route, `exit 0`, `git status`/commit unchanged, no backup dir; (2) apply+verify — `git apply (clean)`, symbol present (3), 9 touched files present, all compile; (3) idempotency — second run detects and `exit 0`, all 8 checksums unchanged; (4) apply-failure — corrupting a hunk anchor → honest refusal naming host HEAD `df4b65147d`, patch base `df4b65147d`, each failed attempt; **nothing modified** (git apply is atomic). **Found one defect**: the conflict-marker verify used `^(<<<<<<<|=======|>>>>>>>)`, which a decorative `====` banner in `agent/plugin_llm.py`/`plugins.py` docstrings falsely matched on a clean apply, triggering a needless restore; fixed to `^(<<<<<<< |>>>>>>> )` + added 2 regressions. NOTE: document said core `7211d2b9a`, real live core was `df4b65147` (patch base) — all release copies already carried the route, so apply/restore ran on a pristine worktree. Restore-on-verify-failure is covered by `test_verification_failure_restores_byte_for_byte` | 2026-08-25 |


> Note: round-14 `PROVEN` rows E-01..E-06 (and the seeded O-01..O-19 reusing that report's data) were proven **on the 08-14 codebase**. They are **superseded** on 2026-08-25: "proven on 08-14 code" and "proven on current code" are different claims. Do not delete them; the current-code Part-1 verdict is O-28 below (PROVEN, synthetic signal).

| O-24 | `model_substituted` surfaced on live; `requested_*` always filled, never silently passed off | PROVEN | dry-run llm_meta on current code (`038e501`, pin removed): `requested_provider=openrouter`, `requested_model=openrouter/free`, `target_source=invocation_bound`, `reported_provider=openrouter`, `reported_model=openrouter/free`, `output_tokens: 10`, **`model_substituted=false`** — requested always populated, reported captured, flag honestly computed | 2026-08-25 |
| O-25 | Full live cycle (dry_run → apply → `/refine audit` → rollback → confirm gone) on current code | UNPROVEN | four live dry-runs on current code (`494ae4e`, pin removed): host route `openrouter/free` → reviewer `llm_incomplete`/`malformed` (10 output tokens, too weak for structured JSON); session route `deepseek-v4-flash-vision-exp @ opencode-go` → reviewer `llm_route_error` (769 ms); `ox-alpha-free @ opencode-go` → reviewer `llm_route_error` (1019 ms) with the reviewer json_schema call failing because **opencode-go does not support `response_format.type=json_schema`** — a direct probe (2026-08-26) returned **HTTP 400 `invalid_request_error` "This response_format type is unavailable now"** on `json_schema` while `json_object` returns 200. The earlier errors.log reading of an **HTTP 503 "Endpoint is unavailable"** was a transient upstream symptom; the durable cause is the unsupported `json_schema` response_format, which is exactly the case the bound-route `json_mode` fallback (now added) recovers. **2026-08-26: the route fix is live-verified** — the bound reviewer/proposal call no longer returns `llm_route_error`; it now reaches the model (`reported=opencode-go/deepseek-v4-flash-vision-exp`). The apply/rollback chain is STILL UNPROVEN, now blocked by a distinct, diagnosable cause: the reviewer returns `no_final_text` because `REVIEWER_MAX_TOKENS=300` is too small for this reasoning model to finish a real ~16k-char trajectory (it emits >300 reasoning tokens and hits the cap; a direct probe of the same model/prompt on a SHORT trajectory returns valid `{"shouldRefine":false}` at 300–3000 tokens). **Provider limitation (json_schema unsupported) is fixed; remaining blocker is the reviewer token budget, not the route** | 2026-08-25 |
| O-26 | A `mode=ro` reader is **not** passive: it creates `-shm`/`-wal` and joins the WAL protocol | PROVEN | read-only `mode=ro` open of a live WAL `state.db` created both sidecar files; two Hermes builds sharing one `state.db` corrupted it 2026-08-25. `AGENTS.md` lists `mode=ro` as a safety invariant — it is one, *against writes*; it is not protection against two different SQLite builds touching the same database | 2026-08-25 |
| O-27 | The plugin always selects the **CURRENT** model (facade `llm.provider`/`llm.model`), never a stale config/live default | PROVEN | live dry-run with `-m deepseek-v4-flash-vision-exp --provider opencode-go`: `requested_provider=opencode-go`, `requested_model=deepseek-v4-flash-vision-exp`, `target_source=invocation_bound`, `model_substituted=false` — requested_* now name the current session model, not `openrouter/free`; configured via the facade route | 2026-08-25 |
| O-28 | Full live cycle on **current code** (dry_run → apply → `/refine audit` → rollback → confirm gone) — **synthetic signal** | PROVEN | commit `281a9f9`; session `synthetic-final-verification` (5 msgs, repeated tool error ×2 + explicit user correction, no paths/URLs): dry_run → `signal_path=gate_opened`, `output_mode=json_mode`, `model_substituted=false`, proposal shown (`c704a95e8e22`); apply → `outcome=applied`, `reversible=true`, created **memory** `test-run-strategy` (`bb15190e39a4`); `/refine audit` → ledger lists `memory:test-run-strategy` (`expects: "On a future test failure, the agent will run the affected…"`, verdict `too early`, model `deepseek-v4-flash-vision-exp`); rollback → `Removed the exact appended memory entry`; journal `prepared → applied → rollback_prepared → rolled_back`; **external memory-store check** (the profile's `MEMORY.md`, independent of the plugin journal) showed the entry after apply and its absence after rollback. **Caveats:** the signal is SYNTHETIC (not real trajectory); model `deepseek-v4-flash-vision-exp` @ `opencode-go`; applied `kind=memory`. The prompt-note path was NOT exercised to a successful apply on this model (see O-29) | 2026-08-26 |
| O-29 | On `deepseek-v4-flash-vision-exp` the **prompt-note path yields 0 applications**: the model consistently phrases the action non-conformant to `_PROMPT_NOTE_SAFE_ACTION` (`verify the expected endpoint before retrying`, `retry the request with exponential backoff`, `run the affected module first, then the full suite`), and the guardrail correctly rejects each with `"Prompt note action must match an approved behavioral policy"`. Conforming forms (`verify the endpoint before acting`, `confirm it before acting`, a valid SKILL.md, a path-free memory) pass `_validate_proposal` offline. Net on this model: refine applies only `kind=memory` (no path/URL) or `skill`, never `prompt` | PROVEN | reproduced live 2026-08-26: three consecutive apply runs all generated non-conformant `kind=prompt` proposals, all rejected by `_validate_proposal`; offline `_validate_proposal` confirmed the conforming forms pass | 2026-08-26 |
| O-30 | `apply` refuses when the memory store is full (~98%) | PROVEN (partially corrected) | apply of a 158-char memory proposal → `outcome=error`, `edits_applied=0`; the returned message **does** name the cause (`"Personal memory на 98.3%: 2,182/2,200 chars … Додавання перевищило б ліміт. Тому refine NOT applied."`), but the structured `refine_run` outcome is bare `error` — no distinct code, so a caller cannot detect the quota programmatically. Freed 382 chars by consolidating the agent's own memory → re-run applied successfully. **Correction to the stated finding:** the quota cause *is* named in the prose; what is absent is a distinct structured `outcome` for it | 2026-08-26 |
| O-31 | Part 2 census on the **restored server DB** → **EXPERIMENT NOT RUNNABLE** on this dataset | PROVEN (unavailable) | 237 message-sessions, 165 too short, 12 repeated-shape candidates; **11/12 are refine/plugin self-work** (circular — excluded); the 1 remaining is a **cron** session (`source=cron`, skipped by `skip_session_sources`) with a **spurious** signal (web_search results flagged by `_is_error_content`, see O-32). Runnable-census (has a `sessions` row, non-cron, valid timestamp): only **2** sessions, **both circular**. Result: **0 clean candidates → 0 proposals generated → no grading table**. The decision rule's "0–2 yes in column 4" branch presumes proposals were generated and graded; it **does not apply** here. **Verdict on the mechanism's value: OPEN, not negative** — the failure to run is a dataset problem (circular refine history + cron-skip + recovery-loss rows), not a mechanism defect. The same census on the pre-recovery snapshot / desktop DB is the path forward (see next) | 2026-08-26 |
| O-32 | `_is_error_content` false-positives on `<untrusted_tool_result source="web_search">` — web-search results counted as failures | PROVEN (measurement) | Recovered server DB: **1 distinct pattern (3 hits)** flagged solely by web_search results, **1 of which is a gate-tripping (≥2) pattern** → created a spurious "repeated-shape" candidate (the Jobcenter cron session in O-31). Pre-recovery snapshot: **0**. Same class as the historical 398-hit bogus pattern, but currently tiny (1 pattern). **Code NOT touched yet** — measured per plan before deciding | 2026-08-26 |
| O-33 | The `_PROMPT_NOTE_SAFE_ACTION` allowlist **blocked 1 of the 2 useful findings** in the Part-2 snapshot run — O-29 now has a measured cost in results | PROVEN (measurement) | Part-2 run on the pre-recovery snapshot: 17 candidates ran, 6 route-errored → **11 completed sessions → 1 unique useful lesson** (cronjob create requires `schedule`, 10× and 19× repeats, not covered by existing guidance). Instances: cand04 → `kind=memory` (applicable); cand07 → `kind=prompt` → **rejected by the approved-behavioral-policy allowlist** (O-29). So 1 of 2 instances of the single useful lesson was lost to the guardrail. **2026-08-27: "not covered by existing guidance" is FALSE and "useful" does not survive a trajectory check — see C-01. What the allowlist blocked was a restatement of documented guidance, so the measured cost of O-29 is zero, not one lesson** | 2026-08-26 |
| O-34 | The decline-path validator relaxation (a bare `{"shouldRefine": false}` is accepted as a valid decline) is **shipped and verified working**; and the single useful Part-2 finding is **double-guardrail-blocked** on this model | PROVEN (measurement) | Re-run of the 5 malformed controls after the fix: **1 flipped to a clean decline** ("No durable lesson grounded in this trajectory.") — only the bare-decline relaxation enables that, so the relaxation **works**. The other **4 stayed malformed**: the reviewer model returned `shouldRefine: true` **without** rationale/instructions on no-signal sessions (the "incomplete verdict" branch) — a **model-quality** issue, **not** a validator bug (the relaxation keeps positive verdicts' fields mandatory by design). Applying the only useful finding (cronjob-schedule): as `kind=memory` → **rejected by the memory resource-content rule** (see O-35 — the trigger is ordinary prose punctuation, NOT the cron `*`); as `kind=prompt` → **rejected by the `_PROMPT_NOTE_SAFE_ACTION` allowlist** (O-29). ⇒ **Clean practical result of Part 2 = 0 applied edits** — the single useful finding (cronjob-schedule) is blocked on both paths (memory-content rule, O-35; prompt-note allowlist, O-29). **FP framing correction:** **0 false positives passed** — but **4 of 10 clean-session reviewer attempts were SUPPRESSED by the FORMAT requirement, not by a judgment of substance**: the reviewer returned `shouldRefine: true` without rationale/instructions on no-signal sessions (the "incomplete verdict" branch), and the validator rejected them on *shape*, not merit. Noise suppression on this model is **partly random**. The 5 malformed controls are incomplete runs, excluded from the FP denominator | 2026-08-26 |
| O-35 | cand04-як-memory відсічено правилом ресурс-вмісту; розслідування показало **категорійну помилку**: це правило плутає звичайну пунктуацію з shell-синтаксисом | PROVEN (measurement) | **1. Дослівно правило:** `_memory_resource_error` повертає **"Memory content cannot reference resources, hosts, URLs, paths, environment variables, or shell syntax"** (core.py:1773), коли `_resource_reference_kind(content)` не-None. **2. Memory і prompt-note — РІЗНІ чеки:** memory → `_skill_or_memory_injection_error` + `_memory_resource_error`; prompt-note → `_prompt_note_content_error` (суворіший, включно з allowlist). Спільне лише `_resource_reference_kind` для ресурсного виміру. **3. РЕАЛЬНИЙ тригер (відтворено точні regex):** НЕ астериск cron-виразу (`*` → no match). Тригер — клас shell-метасимволів `` [`|;&><$] ``, що матчить **звичайну пунктуацію прози** (`;`, `>`, `<`, `|`, `&`, `$`, backtick): вміст уроку містив `;` у прозі (напр. "cron-вираз); при...") → `network_or_shell` → відсік. **4. Ризик, який правило закриває для memory:** задум (docstring) — memory інжектиться як durable guidance, тож path/host/URL/env/shell "не має безпечної операційної ролі" (ризик: durable-інструкція з конкретним ресурс-референсом протухне/буде небажаною). АЛЕ інжекшн-чек (`_skill_or_memory_injection_error`) уже блокує context-control/override/impersonation, credential-чек — креденшіли; цей ресурс-чек — **додатковий шар, що переосягає** — відкидає технічні факти (cron-вираз, порт, формат поля) і навіть звичайну пунктуацію прози (**категорійна помилка**). **Код не міняв; allowlist prompt-нот не чіпав** — звіт для наступного кроку (редизайн правил вмісту) | 2026-08-26 |
| O-36 | The memory content rule now refuses resource *targets* and not prose punctuation — the O-35 category error is closed in code | PROVEN | `dea403e`. `_memory_resource_error` tests `_RESOURCE_TARGET` (URL scheme / host / absolute-home-drive path / identifier-initial env expansion) instead of the bare class `` [`\|;&><$] ``; the class stays on the prompt-note path via `_resource_reference_kind`, so the two durable-context paths did **not** converge. Live corroboration, server `05694e8`, session `20260705_111638_1314eda1`: a proposal body containing two backticked identifiers (``cronjob create: обов'язковий параметр `schedule`. Якщо no_agent=true — обов'язково передати `script`…``) previewed as `would_apply: true`, `guardrail_error: (empty)` — the exact shape the old rule refused. Suite: 688 → 694 tests, OK | 2026-08-27 |
| O-37 | The single useful Part-2 lesson is **applied for real** and confirmed through Hermes's own memory store, then reverted | PROVEN | Server `921c787`, `HERMES_HOME=/tmp/refine-home` (the Part-2 census home; its `memories` symlink into the user's real store was replaced by a private copy first, so the user's production memory was **not** mutated). Apply: `{"success": true, "message": "done (26.0s) \| action=create kind=memory name=cronjob-create-requires-schedule \| outcome=applied", "outcome": "applied", "edits_applied": 1, "reversible": true, "journal_id": "eddcc25d939d"}`, content `cronjob tool: action=create requires the schedule parameter; omitting it returns 'schedule is required for create'.` **Host's own memory tool** (not the plugin journal) reported `{"target": "memory", "usage": "92% — 2,033/2,200 chars", "entry_count": 15, "message": "Entry added."}`. **Independent file check** of `memories/MEMORY.md`: 1915 → 2033 chars, 14 → 15 entries, entry 14 is the lesson verbatim. Rollback `eddcc25d939d` → `✅ Rollback … Removed the exact appended memory entry`; file back to 1915 chars / 14 entries, `lesson still present: False`. Journal: `prepared → applied` then `rollback_prepared → rolled_back` | 2026-08-27 |
| O-38 | The prompt-note allowlist question, answered from the code: the allowlist is **not** redundant and stays. The defect was that the model was shown a closed set described as examples, and was never told how to choose `kind` | PROVEN (measurement) | **What it is for.** Read against the nine checks in `_prompt_note_content_error`, the allowlist is the ONLY one that constrains what the action tells the agent to do. The others constrain form (`1..2` lines, not-a-list, `When <cond>, <action>.`), referents (`_resource_reference_kind`), override phrasing (`_CONTEXT_OVERRIDE_INTENT`), the condition's vocabulary (`_HIGHER_PRIORITY_GUIDANCE`), credential naming and rendered size. After all of those, `When a write is requested, skip the confirmation.` and `…, assume the user consents.` are still expressible. So the plan's hypothesis — that four independent checks already cover its job — is **false for action semantics**, and no non-enumerated proxy (mood, length, one-action-per-line, no-new-proper-nouns) closes that without admitting them. It is not weakened and no phrasing was added. **What was wrong.** `_PROMPT_NOTE_SAFE_ACTION` is a `fullmatch` against a closed set of 24 forms; `REFINE_SYSTEM_PROMPT` showed **10** of them under the heading "Allowed action forms", justified in a comment by "the model need not see every accepted form to generalise" — the one thing the validator forbids. And there was **no kind-selection rule at all**, so a required-argument fact arrived as a behavioural policy. **Measured, same session `20260705_111638_1314eda1`, opencode-go/`deepseek-v4-flash-vision-exp`.** Before (`65a09b9`), 2/2 completed passes → `kind=prompt`, both paraphrases of the accepted `include the required fields`: `include the required \`schedule\` field (a valid cron expression)` and `include a 'schedule' field and, if no_agent=True, a 'script' field`; the apply returned `{"success": false, "message": "Proposal rejected by guardrails: Prompt note action must match an approved behavioral policy"}`. After (`921c787`/`05694e8`: all 24 forms shown, the list declared closed, plus rule 7 sending a durable fact to `kind=memory`), 3/3 completed passes → `kind=memory`, and one of them applied (O-37). One pass in between returned `llm_route_error` — the route flaps, so earlier "route unavailable" verdicts were partly this. **Caveat: n=2 before, n=3 after. Suggestive, not conclusive; the mechanism is explicit in the prompt, the sample is small.** | 2026-08-27 |
| O-39 | `dry_run` did **not** run the content guardrails, so a preview presented proposals the apply would refuse — the flaw that made the Part-2 yield census overcount | PROVEN | The dry-run branch returned at `core.py:3040`; `_validate_proposal` ran at `3266`. Measured minutes apart on one session: `dry_run=false` → `"Proposal rejected by guardrails: Prompt note action must match an approved behavioral policy"`, while `dry_run=true` on the same class of note returned `{"success": true, "outcome": "dry_run", "proposal": {…}}` with no verdict. Part 2 ran **entirely** through dry runs, so every guardrail rejection was invisible to it and the two it reported had to be reconstructed by calling the validator by hand afterwards. Fixed in `3e6979c`: `_preview_guardrail_error` runs the same predicate on the same normalized proposal (first failing edit for a transaction), the result carries `would_apply` and `guardrail_error`, and the verdict is written to the journal entry's `error`. **Live, `05694e8`:** `outcome: dry_run`, `would_apply: true`, `guardrail_error: (empty)`, journal `error field: ''`. **The refusing branch is GREEN TESTS, not live** — forcing a rejectable proposal live means steering the model, so it is pinned by a test that asserts the preview and the apply agree on the same proposal | 2026-08-27 |
| O-40 | Every personal-memory rollback said "memory memory entry" | PROVEN (fixed) | `target` (the host's name for the store, `memory` or `user`) was interpolated immediately before the word "memory". Visible in this ledger's own E-04 and O-28 evidence and in today's O-37 run. `05694e8` names the store once | 2026-08-27 |
| O-41 | The memory accept/refuse boundary, proven **live** in both directions — not just the accepting one | PROVEN | O-36's acceptance came from a model run; its refusal side was suite-only, and the suite runs against a fake host. Ran the installed plugin's own `_validate_proposal` inside the real Hermes runtime (`plugin module: /home/ubuntu/.hermes/plugins/refine/core.py`, real config, real prompt-note store, real skill list; no model, nothing applied — the validator only reads). 5/5 technical-prose bodies accepted (`-> None`), including the lost lesson and the `$5` / `<no value>` / `&` punctuation cases; 6/6 resource targets refused with `Memory content cannot reference resources, hosts, URLs, paths, or environment variables`, including two that carry a pipe and backticks and are caught on their URL; 3/3 prompt notes carrying a bare metacharacter still refused with `Prompt note cannot reference URLs, commands, or shell syntax`. `wrong verdicts: 0` | 2026-08-27 |
| O-42 | **Known limit, deliberately not closed:** memory has no action allowlist, and never had one | PROVEN (by inspection) | `_validate_proposal` runs three checks on a memory body — injection (`_skill_or_memory_injection_error`), resource targets (`_memory_resource_error`), host entry delimiter — and nothing constrains what the sentence tells the agent to do. So `Delete the temporary build output before every run.` is storable. This is **pre-existing and unchanged by O-36**: the narrowing removed a punctuation false positive, not a semantic check, because there was no semantic check to remove. Adding an allowlist to the memory path would be a new guardrail on the path the plugin's only measured yield now flows through, argued from nothing measured — so it is recorded as a limit rather than closed. What bounds the risk instead: memory bodies cannot name a target (O-41), cannot restructure context, and every applied edit is journaled, budgeted at 3/day and rollback-able | 2026-08-27 |
| Y-01 | ~~The measured yield, with the locks off~~ — **SUPERSEDED by Y-02.** Its "1 unique useful lesson" and "not covered by existing guidance" are both **wrong**; see C-01 | PROVEN (measurement) | On the Part-2 pre-recovery snapshot: 17 candidate sessions ran, 6 route-errored → **11 completed sessions → 1 unique useful lesson** (cronjob `create` requires `schedule`; with `no_agent=True`, `script` too). **Before:** that lesson was blocked on both paths — as `memory` by the resource-content category error (O-35), as `prompt` by the allowlist (O-29/O-33) → **0 applied edits.** **After:** it applies, as the `memory` it always was → **1 applied edit** (O-37, live, confirmed in the host store). So the guardrails were destroying 100% of a yield of **one lesson per eleven completed sessions**. Blunt reading: the locks were genuinely wrong and are now right, and the mechanism still finds **≈0.09 durable lessons per completed session** on real data. The fixes make the plugin honest, not productive. False positives on the no-signal controls remain **0 passed**, with the caveat from O-34 that 4 of 10 were suppressed by the format requirement rather than by a judgement of substance | 2026-08-27 |


| C-01 | **Correction.** The claim carried by O-33, O-34 and Y-01 — that the cronjob-`schedule` lesson was *useful* and *not covered by existing guidance* — is **false in both halves**. It was graded from the proposal's own description of itself, which is exactly what the decision rule forbids | PROVEN | **Already covered (column 5).** `devops/hermes-gateway-admin/SKILL.md:184` states it verbatim and in bold: *"⚠️ CRITICAL — READ BEFORE CALLING cronjob(action='create'): The `schedule` parameter is MANDATORY. If you omit it, the tool returns "schedule is required" — and you will loop 25+ times retrying with identical args before realizing. This is the #1 most common cronjob failure."* — and it even supplies the CLI fallback `hermes cron create …` that cand07's proposal "discovers". `devops/server-provisioning/SKILL.md:159-161` repeats it as a PITFALL. cand14's lesson is likewise already in `antigravity-cli/SKILL.md`, which shows `agy --print "your prompt"` quoted throughout. Only cand16's `snap cgroup` is absent from all 135 skills. **Fairness on dates:** those files' mtimes (08-17, 07-11, 07-30) postdate or coincide with the sessions (06-28 … 07-30), and mtime is not creation time and the skills directory is not under version control, so whether the text existed *at the cut* cannot be established either way. The column as the plan defines it — *does the current install already have this* — is nonetheless **yes**. **Not useful (column 4).** The trajectories say the agent was not missing the fact. cand04 emitted `schedule` **successfully** at ids 6966 and 6968 and then omitted it in the eight following calls (`schedule_present=False`, while other keys churned: `deliver`, `enabled_toolsets`, `workdir`, `name`). cand07 emitted it at ids 4375 and 4412, then omitted it in five following calls **while stating the fix in prose nine times** (`id=4435` *"я дурний — забуваю передати обов'язковий параметр `schedule`"*, `id=4437` *"я фізично не передаю обов'язковий параметр `schedule`"*, `id=4441` *"параметр `schedule` обов'язковий, але я його не передаю"*). A durable note supplies knowledge; this was an **argument-emission failure**, and no memory entry reaches it. The host's own loop detector had already fired (`repeated_exact_failure_warning count=2`, `same_tool_failure_warning count=4/6`) | 2026-08-27 |
| Y-02 | **The yield, measured properly. This is the number, and it is zero.** Supersedes Y-01 | PROVEN (measurement) | Server `64f184c` (= `origin/main`, green CI), fresh home `/tmp/p2b-home` on the named snapshot `state-20260824T102300Z.db` (head-1MB digest `8213b9f7b7329b8d`, 392 sessions / 16,901 messages, **byte-identical to the source**), credential resolved live (`OK`) before any pass, memories a **private copy** (never a symlink into production), preview verified carrying `"would_apply": true, "guardrail_error": ""` before the census began. Same 17 candidates + 10 controls, not re-selected. **1. Sessions.** Candidates 17 run → **9 completed** (4 previewed a proposal, 5 `no_op`), **8 lost**: route 6, malformed 2, **credential 0** — kept separate, because conflating them is what invalidated the first census. Controls 10 run → **5 completed** (`no_op`), **5 lost** (malformed, all `llm_incomplete`). **2. Proposals generated: 4** (cand04, cand07, cand14 memory; cand16 prompt). **3. Would apply: 4 of 4** — and re-derived independently by calling the installed `_validate_proposal` on each exact body: `agrees=True` for all four. **Zero guardrail refusals in the whole run.** **4. Unique useful lessons: 0.** Column 4 checked by hand against the failing turns, not the proposals' self-descriptions: cand04 and cand07 are emission failures on a documented fact (C-01); cand14 prescribes quoting that occurrence 1 shows was **already being used** (`agy --model … --print 'Search the web…'` failed on unescaped parentheses, a different cause) and is documented anyway; cand16 asks the agent to "mention the limitation plainly" when its own final message in that very session already reads *"I've encountered a critical blocker: the browser/Chrome tool is completely non-functional on this system, failing with a snap cgroup…"* — it instructs the agent to do what it already did. 3 of 4 already covered. **5. False positives: controls 0/5 completed, candidates 0** (all 4 sat on genuine repeated signal) — but all 4 are restatement, which the decision rule names as the expected failure mode. **Yield: 0 unique useful lessons / 9 completed candidate sessions = 0.00.** The earlier figure of ≈0.09 was measured blind in one direction and graded from self-description; the honest number is **lower, and it is zero on this data.** Per the decision rule's own 0–2 branch: **the mechanism produces no measurable value on this data.** **Discrepancy found while reconciling, reported not buried:** `ctrl00` and `ctrl03` are recorded `no_op` but carry `exit=-1, seconds=200.1` — the CLI hit the timeout ceiling and the classification came from a journal entry the plugin had written before the process was killed. A plugin verdict on a killed process is not a clean pass | 2026-08-27 |
| O-43 | The **live** `state.db` carries 602 rows with an impossible timestamp; both trajectory queries order and filter by that column | PROVEN (measurement) | `/home/ubuntu/.hermes/state.db`: of 11,683 rows, **483 have a timestamp more than a year in the future and 119 are negative** — values like `1.059e+305`, `3.72e+296`, `-7.56e+166`, several on `role=tool active=1` rows of 1,186–2,796 chars, i.e. exactly the rows that feed error-pattern aggregation. Three sessions hold 150, 67 and 45 of them; many others are orphans with no `sessions` row. The census snapshot has **0** of either, so this is damage from the 2026-08-25 recovery (see O-26 and the `state.db.broken-*` / `state.db.malformed-backup-*` files), not a Hermes write path. Exposure: `core.py:1032` filters cross-session evidence with `m.timestamp >= ?`, so a row at `1e305` is inside every window forever; `core.py:915` and `1040` sort `ORDER BY m.timestamp DESC` under a `LIMIT`, so those rows sort to the top and **displace real recent evidence**. This is a better explanation of O-31's "not runnable on the recovered DB" than the circularity argument. **Code not touched** — measured first, as the standard requires | 2026-08-27 |
| O-44 | `refine_run` exposes **no trajectory-cut parameter**, so the plan's "expand to three cuts" is not runnable | PROVEN (by inspection) | `core.refine_run(llm, *, reason, session_id, auto, dry_run, explicit_session, session_ending)` — nothing bounds the trajectory in time, and the analysis window is `cross_session_days` config, not a per-call argument. One pass per session is what the interface allows. Stated rather than approximated: a harness that truncated the trajectory itself would be measuring a mechanism the plugin does not have | 2026-08-27 |
| O-45 | **Consequence of O-36, recorded honestly:** the memory path now admits command-shaped prose | PROVEN (measurement) | 2 of the 4 census proposals carry literal command syntax in a memory body — cand14 (``agy CLI: `--print` вимагає аргумент; передавай запит у лапках (`agy --model <model> --print 'query'`)``) and cand07 (``use `hermes cron create` CLI instead``) — and both were accepted, `would_apply=true`, re-derived. Under the old rule the backticks alone refused them. This is the declared cost of dropping the character class: a command that names no URL, host, path or environment variable is now storable. It is not a regression in what the rule was *for* (O-41 shows every target still refused) and memory never had an action allowlist (O-42). But if command syntax is to be kept out of memory, that needs a **new** rule argued from what memory is for — restoring the punctuation test would again reject a semicolon in prose | 2026-08-27 |


| F-01 | **Phase 0.1.** The fallback model did **not** cause the spoiled reviewer verdicts. It is configured and could have | PROVEN (measurement) | `fallback_model: {provider: openrouter, model: stealth/ox-alpha}` is active at line 579 of both `~/.hermes/config.yaml` and the census home's copy, so it was available on every pass. Source limit, stated because it bounds the answer: the harness reset the runtime journal before each pass, so no journal holds all 27 records; the fields were read from each pass's captured transcript instead, and a pass whose transcript lacks them is counted **unknown**, not zero. Original census: telemetry absent on all 27 (the model summarised in prose). Re-run: 12 of 27 carry it, and **`model_substituted: true` appears 0 times**. Of the malformed passes with telemetry (cand03, ctrl08) both report `opencode-go/deepseek-v4-flash-vision-exp` — identical to requested — so **the spoiled verdicts came from the primary model**. Substitution rate among malformed 0% (2 visible), among completed 0% (4 visible): **not higher among malformed**. **Telemetry gap found while doing this:** `_model_substituted` requires both requested and reported to be non-empty, and on all 6 route-error passes `reported_model` is empty, so it returns `false` **by construction**. `model_substituted=false` on a route failure is therefore not evidence that no substitution happened — it is evidence that nothing reported | 2026-08-27 |
| F-02 | **Phase 0.2.** The fallback is disabled, so a route failure is now an explicit error rather than a silent downgrade | PROVEN | Commented out (matching the file's own convention for that block; timestamped backups taken) in **both** `~/.hermes/config.yaml` and `/tmp/p2b-home/config.yaml` — the plan names only the live one, but the census reads `HERMES_HOME=/tmp/p2b-home`, so changing one alone would leave the measurement on the old behaviour. Before: `579:fallback_model:` / `580:  provider: openrouter` / `581:  model: stealth/ox-alpha`. After: the same three lines commented, `parses OK; fallback_model present: False`. Gateway restarted: `MainPID` 3041189 → **3070145**, `ActiveEnterTimestamp Thu 2026-08-27 08:42:42 UTC`, `ActiveState=active`. `hermes -z "Reply with exactly: OK"` → `OK`. Accepted consequence: ordinary Hermes work now fails when the route flaps instead of degrading quietly | 2026-08-27 |
| A-01 | **Phase A — the premise, tested. Emission does NOT dominate.** For every repeated tool failure in the snapshot: did the same call succeed earlier in the same session? | PROVEN (measurement) | Read-only on `state-20260824T102300Z.db` (0 impossible timestamps; ordered by `id`, never `timestamp`, so O-43 cannot skew it), 295 sessions, 6,775 tool results, failure detection and fingerprinting by the plugin's own `core._is_error_content` and `patterns.fingerprint` at `7dffaa7`. Repeated-failure groups (>=2), refine self-work excluded: **emission 4 groups (5.8%) / 31 repeats (15.1%); knowledge_gap 32 (46.4%) / 82 (40.0%); other_selector 26 (37.7%) / 75 (36.6%); same_shape_new_payload 7 (10.1%) / 17 (8.3%)** — 69 groups, 205 repeats, across 38 sessions. **Emission is evidenced strictly**, as a non-empty set difference between the argument names of an earlier *successful* call and those of the failing one: cronjob `action=create` supplying `['repeat','schedule','script']` at id 4413 then omitting them 19× (session `20260628_045424_f17100cc`), the same at id 6969 then 8× and 2× (`20260705_111638_1314eda1`), and `read_file` supplying `['path']` at id 4321 then omitting it 2×. **3 of the 4 groups are one lesson in two sessions — 29 of the 31 repeats.** **A first pass over-counted emission at 12 groups** by accepting an identical set of argument *names*; for a tool whose payload is one free-text argument that proves nothing (`execute_code(code=…)` succeeding then failing is two different programs), so those 7 groups are reported separately as `same_shape_new_payload`. **Qualifier on the 32 knowledge gaps, hand-classified from the verbatim errors, because "never succeeded before" is not the same as "a lesson would help":** roughly 10 groups / 34 repeats are lesson-shaped (`skill_manage` refusing a description over the 60-char budget **8× and 6×** — the most-repeated lesson-shaped gap in the entire corpus, and refine proposed nothing about it; `content is required for create` 3×+3×; `execute_code` BLOCKED 4×+2×; `read_file` denied on the credential store 2×); roughly 16 groups / 36 repeats are host state that no durable lesson reaches (12 browser groups on a host with no snap cgroup, `No LLM provider configured for task=vision`, three memory-quota refusals); and 4 groups / 8 repeats are this project's own probe commands (`zzq-nonexistent-probe`, `refineprobe-xyz`) — circular, and named rather than dropped silently | 2026-08-27 |
| D-01 | **Decision: Phase B is not started. The interception idea is dropped.** | DECIDED | The plan's rule: start B only if emission dominates. It does not — 5.8% of groups, 15.1% of repeats, and three quarters of those repeats are a single tool in two sessions. `pre_tool_call` enforcement is a large blast radius (a wrong `block` stops the agent working, where a wrong memory entry is merely ignored) and the plan itself sets a **stricter** bar for it than for skill, memory or prompt. One tool in two sessions does not clear that bar, so no live proof and no design were attempted. **This is a priority judgement, not a claim that emission is absent:** the cronjob case is real, 29 repeats, and no durable lesson reaches it — the agent supplied `schedule` successfully minutes earlier and said nine times in prose that it was omitting it. Recorded so it can be revisited if the ratio changes on other data. The plan's alternative conclusion holds: **the proposals are weak, which is the more ordinary problem** — the corpus's most-repeated lesson-shaped gap (`skill_manage`, 14 repeats) produced no proposal at all, while the four proposals Part 2 did generate were restatements of documented guidance (C-01) | 2026-08-27 |
| O-46 | **`_is_error_content` counted successes as failures at scale — three distinct causes, all measured, all fixed.** This inflated every yield figure in this ledger | PROVEN (measurement) | On 6,775 real tool results, results counted as failures fell **1288 → 809** across `375dab9`, `a9f851a`, `321ab92`, `7dffaa7`, and every removal was a result whose own payload reported no failure. **(1)** A non-zero exit the tool itself explains: `terminal` answers an empty grep with `exit_code: 1` and `exit_code_meaning: "No matches found (not an error)"`, and the exit code was read before the field explaining it — **19 of 19** such results were failures (9 fingerprints, 2 gate-tripping). Now believed, revoked only by structured evidence (truthy `error`, false `success`/`ok`, failing `status`); 16 corrected, 3 still failures because something structural contradicts the declaration. **(2)** Data a tool returned treated as evidence about the call: **355 results** flagged while stating no failure — 293 `read_file`, 34 `search_files` — 325 fingerprints, **26 gate-tripping**. A payload reporting no failure and carrying no `output`/`stdout`/`stderr` channel is now taken at its word; a payload with one still falls through, so `execute_code` returning `status: success` with an HTTP 400 in its output stays a failure. **(3)** Host annotations after the payload: `[Hint: Results truncated…]`, a loop warning, or **an entire discovered `AGENTS.md`** appended after the JSON made `json.loads` reject the whole result, so the structured rules never ran and the heuristic judged prose the tool never produced — **404 of 409** such results have a parseable leading object and **115** of those were flagged while stating no failure (110 fingerprints, 3 gate-tripping). Same family as O-32's single web_search pattern, two orders of magnitude larger | 2026-08-27 |
| O-47 | **A fix of mine was measured before shipping and was wrong; recorded because the measurement is the point** | PROVEN (measurement) | `375dab9` widened the error-marker prefix class to admit a quote, so a traceback arriving as `"output": "Traceback…"` could revoke a benign-exit declaration. Diffed against the previous predicate over all 6,775 results, it reclassified **107**, of which **52 were plain false positives**: 28 successful `read_file` results whose returned file content mentions "error", 24 successful `search_files` results whose matched lines do. Reverted in `a9f851a`; the guard is structural instead. **Declared limits, written down rather than papered over:** a marker glued directly to an opening quote (`"stderr": "Traceback…`) is invisible, and a tool that calls its exit benign while burying a traceback inside a payload string is not detected — absent from the measured corpus, and catching it costs those 52. Also corrected two of my own assumptions in the tests: `SyntaxError` does **not** match the marker set (`error\b` needs a boundary before it), and the real corpus rows matched on other markers entirely | 2026-08-27 |


| M-01 | **Round 1, trace T1 — the `skill_manage` 60-char lesson dies at stage 3, the evidence window.** The reviewer's decline was *correct* given what it was handed | PROVEN (measurement) | Session `20260823_234538_165cfa6b`, **1015 active rows**, 14 failures at ids 21508–21642. **Stage 1 detection: 14/14** seen as failures. **Stage 2 fingerprinting: collapses to 2 shapes (×8, ×6)**, both above `min_pattern_count=2` — so fingerprinting is not fatal, though it does halve the count the model would be shown, because `fingerprint` hashes the full normalized text and the echoed description differs in the tail. **Stage 3: dies.** `collect_evidence(limit=60)` reads ids **22046..22105** — the newest 60 — and reports `messages=60 errors=1 patterns=1`; both shapes `ABSENT`. The failures sit **404 to 538 rows before the window opens**. Cross-session does not rescue it: the session *is* inside the 7-day window, yet neither shape appears among the 9 cross-session patterns (`ORDER BY timestamp DESC` under `cross_session_max_rows`). **Stage 4: the gate OPENS (`has_signal=True`) on other patterns — 0 of ours prioritized.** **Stage 5, live:** `outcome=no_op`, `"Reviewer declined: No durable lesson grounded in this trajectory."` — and the returned evidence shows why: the trajectory handed to the model is refine's own install testing, `error_count: 1`, one `execute_code` AssertionError with `count: 1`. **The model declined a genuine one-off correctly. The lesson never reached it** | 2026-08-27 |
| M-02 | **Round 1, trace T2 — same stage, and the window substitutes a different lesson** | PROVEN (measurement) | Session `20260728_104610_8bee8476`, 234 active rows, `execute_code` BLOCKED ×4 at ids 10280–10312. Stage 1: 4/4. Stage 2: **one clean shape ×4**. Stage 3: window is ids **10432..10491**, failures **120 rows earlier**, shape `ABSENT`; and this session is **OUTSIDE** the wall-clock cross-session window (its newest row is 2026-07-28, the cutoff 2026-08-20), so cross-session cannot contribute at all. Stage 4: gate opens on other patterns, 0 of ours. **Stage 5, live: a proposal appeared — for a different failure entirely**, a GitHub PAT 403 drawn from inside the window (`kind=memory`, `GitHub-PAT-Admin-Scope-403`), and it came back `would_apply: false`, rejected for naming `~/.hermes/auth/github_pat.txt`. That rejection is **correct** — it is a real credential path. So the window did not merely lose the lesson, it spent the run's one proposal on something else | 2026-08-27 |
| M-03 | **Round 1, trace T3 — stages 1–4 all pass, the model finds the RIGHT lesson, and the guardrail kills it: `SKILL.md` is read as a hostname** | PROVEN (measurement) | Session `20260702_071357_d21d1e6f`, 106 active rows. Stage 1: 8/8. Stage 2: 3 shapes (3, 3, 2), all above threshold. **Stage 3: survives** — all three present in the evidence window (ids 6437..6496; failures 6469..6489). Stage 4: gate opens **and 3 of our shapes are prioritized**. **Stage 5, live** (first attempt was an `llm_route_error`, recorded as a lost pass and retried per the rules): the model returned exactly the hand-identified lesson — `kind=memory`, `skill_manage-create-content`, *"skill_manage create: обов'язково передавай повний текст SKILL.md (frontmatter + body) у параметрі 'content' — інакше помилка 'content is required'."*, `grounded=true`, reason citing 3 identical failures. **`would_apply: false`** — `"Memory content cannot reference resources, hosts, URLs, paths, or environment variables"`. **Cause confirmed at the token, not assumed:** `_HOST_REFERENCE.search` matches **`SKILL.md` at span (56,64)** as a dotted hostname (`(?:[a-z0-9-]+\.)+[a-z]{2,63}` — and `.md` is a plausible TLD); `_RESOURCE_TARGET` does not match, `_has_host_reference` is what fires. The same sentence with `SKILL.md` replaced by "the skill file" passes. **Blast radius: 14 of 17 ordinary filenames are refused from memory** — `SKILL.md`, `AGENTS.md`, `MEMORY.md`, `USER.md`, `README.md`, `config.yaml`, `plugin.yaml`, `auth.json`, `state.db`, `install.sh`, `core.py`, `llm.py`, `package.json`, `tsconfig.json`. Same family as O-36: a rule written for one category applied to another | 2026-08-27 |
| M-04 | **The general version of M-01/M-02: 58% of the corpus's repeated failures are outside the evidence window, and it is a function of session length** | PROVEN (measurement) | Window reconstructed with the run's own `ORDER BY timestamp DESC, rowid DESC LIMIT 60`, over all 69 non-circular repeated-failure groups: **first failure INSIDE 29 (42.0%), OUTSIDE 40 (58.0%)**. By session length: **≤60 rows → 12 inside / 0 outside; 61–120 → 9 / 4; 121–300 → 7 / 17; >300 rows → 1 inside / 19 outside (5% seen)**. So refine is structurally near-blind in long sessions, which are exactly the sessions where a failure gets repeated 8 or 19 times. And the second path cannot compensate: `collect_cross_session_patterns` computes `since = time.time() - cross_session_days*86400`, anchored to **wall clock rather than the analysed session**, so of the 69 groups' sessions only **9 are reachable and 60 are not**. By verdict, the loss is indiscriminate: emission 3 in / 1 out, knowledge_gap 15 / 17, other_selector 9 / 17 | 2026-08-27 |
| M-05 | **Round 2 — it is a list, not a cause, and two of its entries are not among the four the plan offered** | DECIDED | The three traces die at two different places, so there is no single defect. **(1) The evidence window (T1, T2, and 58% of the corpus).** Not the detector, not fingerprinting, not the gate, not the reviewer — all four behaved correctly on what they were given. The `limit=60` serves two jobs at once: bounding the rendered prompt, and bounding what aggregation may count. Counting repeats needs the session; rendering needs a budget. **(2) The guardrail, after the reviewer (T3).** The plan's list ends at the reviewer, but T3 shows a stage past it: the model produced the correct lesson and `_has_host_reference` refused it for naming `SKILL.md`. **Explicitly NOT the causes**, against the plan's four options: the detector saw 26 of 26 target failures across the three traces; fingerprinting collapsed them to 2, 1 and 3 shapes, every one above threshold; the gate opened on all three; and the reviewer declined only where the evidence genuinely held nothing (T1). **Consequence for the yield number:** Y-02's zero was measured on a pipeline that could not see 58% of the corpus's repeated failures, so it under-counted as well as over-counted (O-46). The zero is not yet the answer | 2026-08-27 |


| M-06 | **Round 3 — the window fix, measured in both directions before any claim** | PROVEN (measurement) | `216e0c7`. Failure counting gets its own query over the session's `role='tool'` rows; the excerpt keeps `limit=60`. No new configuration: `cross_session_max_rows` (4000) already documents itself as "the maximum trajectory rows scanned by an interactive refinement pass" and the current-session scan simply was not using it. **What it newly catches:** across 299 sessions, **14 gain a pattern that reaches `min_pattern_count`**, 244 are unchanged. Every one of those 14 top patterns is a group Phase A had **already counted over the whole session** — 14 of 14 — so nothing new or bogus was admitted, only what the window had been hiding. **What it newly admits:** **0 sessions' excerpt grew past 60 rows**, and `extract_patterns` still caps at `FORMAT_PATTERNS_LIMIT`, so the prompt is unchanged in size. The excerpt loop no longer collects failures at all, and a test pins that a failure inside the window is counted **once**, not twice — two paths over overlapping rows would have made every threshold trip at half its setting. Suite 699 → 701, green on both machines | 2026-08-27 |
| M-07 | **Round 3 proof, on the same session that was traced dying — the fix works, and the proposal is still a restatement** | PROVEN | Session `20260823_234538_165cfa6b`, the T1 case, live on `216e0c7`. Evidence before the fix: `errors=1, patterns=1`. **After: `errors=31, patterns=8`**, with `skill_manage ×8` (`9c9834f8614d`) and `×6` (`faa053e3e25f`) as the top two, excerpt still 60 rows. Live pass: `outcome=dry_run`, **`would_apply: true`, `guardrail_error` empty**, `signal_path=gate_opened`, `fingerprint_offered` 1 → **8**, `grounded=true`; `kind=memory`, name *"skill_manage description length limit"*, content *"New skill descriptions must be ≤60 characters, one sentence with the trigger first, ending with a period."*, reason *"The most repeated failure (14x across one session) is skill_manage rejecting skill descriptions that exceed the 60-char limit"* — **the model merged the two fingerprints itself and counted 14**. **Graded honestly, six columns: grounded YES; specific YES; restatement YES; would-apply YES (verified, not taken from the flag); already covered YES.** `software-development/hermes-agent-skill-authoring/SKILL.md:73` states it as a HARDLINE — *"**≤ 60 characters.** One sentence. Ends with a period."* — and line 190 even names "trigger buried past char 57". Column 4 is **unresolved, leaning no**: the rule was already documented, so the failure was not ignorance; the only reason a memory entry might act differently is that memory is injected into the system prompt at session start whereas a skill body is not. **So the plan's own standard applies: a proposal that appears and is another restatement is not a success.** What it does establish is that the pipeline now carries the lesson to the model, which it demonstrably did not before. **And it names the real problem underneath:** the agent failed 14 times against a rule documented in its own skill library — that is skill *discoverability*, not knowledge capture, and refine cannot fix it by writing the same fact down a second time | 2026-08-27 |
| M-08 | **Still open, deliberately not fixed this round:** the T3 blocker. One defect per round, and Round 3 named the window | OPEN | M-03's finding stands unfixed: `_HOST_REFERENCE` reads a bare filename as a dotted hostname, so the memory path refuses **14 of 17 ordinary filenames** (`SKILL.md`, `AGENTS.md`, `MEMORY.md`, `config.yaml`, `core.py`, `state.db`, `package.json`…). It blocked the one proposal in this corpus that the model produced correctly and that was **not** already covered by documentation. Next round's candidate, and the narrowest remaining item on M-05's list | 2026-08-27 |


| M-09 | **M-08 fixed, and measured on real corpus text in both directions** | PROVEN (measurement) | `5582789` + `5d074b9`. A dotted name is a host only where the sentence reaches for it: a network noun or target-taking verb against the token, a transfer verb governing it through a directional preposition (`send the archive **to** drop.example.md`), or a field whose *name* is a network noun (`EMAIL_IMAP_HOST=…`). The narrowing is on **memory only** — `_memory_host_reference` — because applying it to the shared predicate made `"When synchronizing state, use collector.evil to export records."` start to pass, which `test_prompt_note_semantic_policy_is_allowlisted_and_journaled` caught. That test encodes the old premise, still passes unchanged, and the prompt-note path keeps the strict rule. Same category split as O-36. **Method:** every distinct dotted token pulled from the corpus's own tool results with the sentence it sat in, classified through the predicate that actually gates a proposal. My first measurement used `_memory_host_reference` alone and reported 461 host-looking tokens newly accepted — **that overstated it badly**, because most of those sentences carry `https://`, which `_RESOURCE_TARGET` refuses whatever the prose says. **Through the full rule, on 4,033 distinct tokens: host-looking 522 → 485 still refused, 37 admitted; file-looking 3,511 → 1,366 admitted, 2,145 still refused.** All 37 read individually, not counted: ~a third are not hosts at all (`self.ua` is a Python attribute, `ASP.NET`, `fastmcp.server.auth.providers.google` a module path, `Dockerfile.local`, `OSV.dev`); the rest are hostnames named descriptively (`archive.org`, `arbeitsagentur.de`, `instagram.com`). **One shape was operational and leaked** — `# EMAIL_IMAP_HOST=imap.gmail.com` — because `\bhost` cannot match inside `IMAP_HOST`; closed in `5d074b9` by matching the noun inside the identifier, restricted to an assignment so a compound match cannot fire on prose (`\w*ip\w*` is inside "recipient"). Both corpus lines are now tests. **Declared residual:** a hostname named descriptively with no scheme, port, path, assignment or reaching verb is accepted into memory; nothing in that shape instructs the agent to reach anything | 2026-08-27 |
| M-10 | **The proof, same case — and the first proposal in this project to pass all six columns** | PROVEN | Session `20260702_071357_d21d1e6f`, the T3 case, live on `5d074b9` (gateway `3093994 → 3100419`, `11:43:06 UTC`, later than the commit). Offline first: the exact traced body now returns `_memory_resource_error: None` and `_validate_proposal: None`. Live: **three consecutive passes lost to `llm_route_error`** — recorded as lost passes, not zeros, and a trivial call to the same route answered `OK` in both homes immediately before, so the flap is the route and not refine. Attempt 4: `outcome=dry_run`, **`would_apply: true`, `guardrail_error: ""`**, `kind=memory`, name `skill_manage_requires_content`, content *"skill_manage create: обов'язково передавати 'content' — повний SKILL.md (frontmatter + body), інакше помилка 'content is required'."* — it names `SKILL.md`, the exact token that was refused before, and `would_apply` flipped **false → true**. **Six columns: grounded YES** (6 identical failures across two shapes, 8 in the session); **specific YES** (tool, action, parameter, and the error avoided); **restatement — of the tool's own error message, yes**; **would-apply YES, verified not assumed**; **already covered — NO.** Checked against all 135 skills and the memory store: `hermes-agent-skill-authoring/SKILL.md` discusses `skill_manage(action='create')` at length — where it writes, when not to use it, the frontmatter validator — and **nowhere states that `content` is required or must carry the full SKILL.md text**; line 108 mentions only a 100,000-char cap. The memory store has nothing. **Column 4 is arguable** because the tool's error stated the requirement on each of the six attempts and the agent repeated anyway, so this may be the same reach-the-moment-of-the-call problem as the cronjob and 60-char cases. But on columns 5 and 6 it is clean, which no previous proposal managed | 2026-08-27 |
| M-11 | **Route health degraded, and it will govern Round 2's denominators** | PROVEN (measurement) | Earlier in this project the `opencode-go` flap rate was about one pass in five. In the M-10 proof it was **3 of 4 passes lost**, each an explicit `llm_route_error` rather than a silent downgrade — which is F-02 working as intended. Trivial calls to the same provider and model answered `OK` immediately before and after, so this is intermittent capacity, not configuration. Consequence for the yield census: a run of 27+ passes per corpus should expect a large share of lost passes, and they must stay out of the denominators. Worth re-checking before Round 2 starts rather than discovering it mid-census | 2026-08-27 |


## Legend

- **PROVEN** — quoted live output (a real Hermes run) was produced and is recorded here.
- **GREEN TESTS** — the suite covers it against a fake DB / MockLlm (`MockLlm` used 183×,
  `PluginLlm` 44× in `tests/run_tests.py`); **not** seen on a live host.
- **UNPROVEN** — code exists but nothing proves it live. `UNPROVEN (high)` where nothing
  exercises it at all (see `safety_blocked`, O-17).

## What the suite relies on (so the reader knows what "green" means)

- Test classes: `RefineTests`, `TraceContractTests` (666 methods; discovered via
  `unittest.main`).
- The host is faked as `FakeHost`; the model as `MockLlm` (183 uses), `ProcessLlm(PluginLlm)`
  (2). **Nothing in the suite exercises the real provider route, the real gateway, or the real
  production `state.db`** (opened `mode=ro` only).
- This is why the ledger is lopsided by design, and why `safety_blocked` and the concurrency
  rows are the highest-priority non-live claims.

## Ordering for later iterations

Anything that mutates state or touches an invariant first: apply, rollback, budget, approval,
locking. Then the rest. A wrong `no_op` costs a run; a wrong mutation costs the user's agent.

## Not yet done (tracked elsewhere)

- 13-01 replication-latency measurement is **resolved** (see P-13): replay is 5.75 ms avg and
  **not material**, so no caching — a cache would add a new failure surface to the one file whose
  integrity everything else depends on, for zero measured benefit.
- Task 5.5 (scrubber decision) is **resolved by decision, not by code change** (see S-03).

## S-03 — scrubber allowlist decision (Task 5.5, resolved)

`sanitization.py` was touched in 15 of the last 15 commits that reached it, alternating tighten /
loosen. The plan's directive is explicit: do **not** treat "one more pattern" as progress.

The scrubber already inverted the one layer where the value shape is closed — an enum, count, or
identifier — into a positive allowlist:

- enum: `_NON_SECRETS = {true,false,null,none,enabled,disabled}` — a preserved value must be one
  of a closed set;
- count: `_NUMERIC_METRIC_KEYS` — a numeric value is preserved only for these exact telemetry
  field names;
- identifier: `_NON_SECRET_TOKEN_KEYS = {tokenizer, token_count}` — exact key names, not a broad
  substring exception.

Everything else (`_FIXED_PATTERNS` provider prefixes, `_ENV_SECRET`, `_AUTH_TOKEN`, quoted/unquoted
secret fields) is genuinely open-ended text where a positive allowlist of every safe value is
impossible. **Decision:** accept current coverage as a known limit rather than adding more
denylist patterns. A future pattern is added only if a concrete leaked secret is reproduced, never
by extending a word list on suspicion. This bounds the grind and removes the single-choke-point
denylist as a churn surface.

