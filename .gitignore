# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Runtime data (may contain trajectory/session data)
refine_journal.jsonl
refine_journal.jsonl.pre-update-*
skill_stats.json
skill_stats.json.pre-update-*
prompt_notes.json
model_override.json
backups/
*.bak

# Secrets / local config
.env
.env.*
.envrc
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.secret
*.token
*token*.txt
*token*.json
*credentials*.json
*credential*.json
*oauth*.json
*secrets*.yaml
*secrets*.yml
*secrets*.json
config.local.*
*.local.yaml
*.local.yml
*.local.json
hosts.yml
id_rsa
id_ed25519
id_dsa
id_ecdsa

# Auth / token directories
.auth/
.aws/
.gcloud/
.ssh/
credentials/
secrets/

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp

# Tooling / agent scratch — never committed. A permanently dirty tree hides
# real accidental modifications among noise, and this loop's crash-recovery
# story depends on reading `git status` and trusting it.
.coverage
.pytest-cache/
.kiro/
scratch/
semantic-review/
*.bundle

# Throwaway planning artifacts from the refine loop. Deliberate, durable plans
# belong under docs/ instead of cluttering the tree root.
plan-*.md
